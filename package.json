{
  "name": "deltasports-monorepo",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace web",
    "dev": "npm run dev --workspace web",
    "start": "npm run start --workspace web",
    "lint": "npm run lint --workspace web",
    "test:functions": "npx tsc -p supabase/functions/tsconfig.json && node --test supabase/functions/tests/edge-functions.test.mjs"
  }
}
